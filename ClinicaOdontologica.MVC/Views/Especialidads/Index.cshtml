@model IEnumerable<ClinicaOdontologica.Modelos.Especialidad>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombreEspecialidad)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Odontologos)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.nombreEspecialidad)
            </td>
            <td>
                @Html.DisplayFor(model => item.descripcion)
            </td>
            <td>
                @Html.DisplayFor(model => item.Odontologos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idEspeciliadad">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idEspeciliadad">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idEspeciliadad">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
